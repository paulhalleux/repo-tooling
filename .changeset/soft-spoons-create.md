---
"@paulhalleux/repo": minor
"@paulhalleux/vite-config": minor
---

Add `repo create` with catalog-backed `app/react` and `library/react`
scaffolds. Add a shared React configuration package for Vite applications.
