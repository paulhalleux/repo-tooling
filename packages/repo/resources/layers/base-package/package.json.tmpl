{
  "name": "{{packageName}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "license": "{{license}}",
  "type": "module",
  "scripts": {
    "check": "pnpm run typecheck && pnpm run lint && pnpm run format:check"
  }
}
