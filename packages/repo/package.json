{
  "name": "@paulhalleux/repo",
  "version": "0.3.2",
  "description": "Repository bootstrap, synchronization, validation, migration, and project-scoped AI tooling.",
  "type": "module",
  "bin": {
    "repo": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "resources"
  ],
  "scripts": {
    "build": "pnpm run check:resources && tsc -p tsconfig.json",
    "check": "pnpm run check:resources && tsc -p tsconfig.json --noEmit",
    "check:resources": "node scripts/check-resources.mjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@paulhalleux/scaffold": "workspace:*",
    "commander": "^14.0.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/paulhalleux/repo-tooling.git",
    "directory": "packages/repo"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "engines": {
    "node": ">=22"
  }
}
